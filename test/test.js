
const f = (
  a : number,
  b : number
) => a + b;

export default from './test.js';

const createElement = () => {};
const Foo = () => <div/>;
const jsx = <Foo/>;

/* eslint no-console: 0 */
if (jsx) {
  console.log(f(1, 2));
}
